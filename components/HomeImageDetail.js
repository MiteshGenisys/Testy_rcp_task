import React from 'react';
import { View, Image, Text, } from 'react-native';

const HomeImageDetail = props => {
    return (
        <View>
            <Text>Home image detail screen</Text>
        </View>
    );
};

export default HomeImageDetail;